import React from 'react';
import styled from 'styled-components';

export const IfrmReloadButton = () => {
  return (
    <SButton>更新</SButton>
  )
}

const SButton = styled.button`

background-color: red;
`;